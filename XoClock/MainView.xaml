<Window x:Class="XoClock.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XoClock"
        mc:Ignorable="d"
        Title="MainWindow" Height="99" Width="270"
        BorderThickness="0" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        ResizeMode="NoResize" 
        Topmost="True"
        Margin="0" Padding="0"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        >
    <Grid ClipToBounds="True">
        <Viewbox Stretch="Uniform" Margin="0" ClipToBounds="True">
            <Label TextOptions.TextRenderingMode="Grayscale" HorizontalAlignment="Center" VerticalAlignment="Center"
                   ClipToBounds="True"  Padding="0" Margin="0" 
                   Background="Gray" Foreground="DarkBlue"
                   Content="{Binding Path=DisplayTime, FallbackValue='08:54:01'}"/>
        </Viewbox>
    </Grid>
</Window>
