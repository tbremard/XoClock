<Window x:Class="XoClock.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XoClock"
        mc:Ignorable="d"
        Title="MainWindow" Height="100" Width="300"
        BorderThickness="0" WindowStyle="None" AllowsTransparency="True" 
        ResizeMode="NoResize" Background="Black" Topmost="True"
        Loaded="Window_Loaded"
        >
    <Grid>
        <Viewbox Stretch="Uniform">
            <Label HorizontalAlignment="Center" VerticalAlignment="Center" ClipToBounds="True" Foreground="Aqua" Padding="0" Margin="0" Content="{Binding Path=DisplayTime, UpdateSourceTrigger=PropertyChanged}"/>
        </Viewbox>
    </Grid>
</Window>
